import XCTest

// Placeholder — expanded in Task 2.
// The VideoAnalyzer type must exist for this to compile.
final class VideoAnalyzerTests: XCTestCase {
    func testPlaceholder() {
        // expanded in Task 2
    }
}
